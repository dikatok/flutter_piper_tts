# Run with `dart run ffigen --config ffigen.yaml`.
name: FlutterPiperTtsBindings
description: |
  Bindings for `src/flutter_piper_tts.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/flutter_piper_tts_bindings_generated.dart'
headers:
  entry-points:
    - 'src/flutter_piper_tts.h'
  include-directives:
    - 'src/flutter_piper_tts.h'
ffi-native:
comments:
  style: any
  length: full
